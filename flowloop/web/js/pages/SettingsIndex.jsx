import React from 'react';

const SettingsIndex = ({ showIndex }) => (
  <div className="settings-layout _page-left">
    settings

    <a onClick={showIndex}> back </a>
  </div>
)

export default SettingsIndex
